enum SocketEvents {
    Test = 'test',
    Join = 'join',
    Leave = 'leave',
    Error = 'error',
    TicketJoin = 'ticket:join',
    TicketLeave = 'ticket:leave',
    TicketChange = 'ticket:change'
};


export default SocketEvents;